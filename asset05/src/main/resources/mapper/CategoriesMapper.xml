<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.asset05.mapper.CategoriesMapper">
    <resultMap id="CategoriesMap" type="com.system.asset05.entity.Categories">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getCategories" resultMap="CategoriesMap">
        SELECT id, parent_id, name, remark, create_time, update_time
        FROM asset_categories
        ORDER BY parent_id ASC, id ASC
    </select>

    <insert id="addCategories" parameterType="com.system.asset05.entity.Categories"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO asset_categories (parent_id, name, remark, create_time, update_time)
        VALUES (#{parentId}, #{name}, #{remark}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateCategories" parameterType="com.system.asset05.entity.Categories">
        UPDATE asset_categories
        SET parent_id   = #{parentId},
            name        = #{name},
            remark      = #{remark},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteCategories" parameterType="int">
        DELETE
        FROM asset_categories
        WHERE id = #{id}
    </delete>

    <select id="countChildrenByParentId" parameterType="Integer" resultType="int">
        SELECT COUNT(*)
        FROM asset_categories
        WHERE parent_id = #{parentId}
    </select>
</mapper>